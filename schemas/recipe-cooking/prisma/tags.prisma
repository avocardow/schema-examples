// tags: Freeform labels for categorising and filtering recipes.
// See README.md for full design rationale.

model Tag {
  id        String   @id @default(uuid())
  name      String   @unique
  createdAt DateTime @default(now()) @map("created_at")

  recipeTags RecipeTag[]

  @@map("tags")
}
